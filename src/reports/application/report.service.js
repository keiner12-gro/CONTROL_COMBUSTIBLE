class ReportService{constructor(reportRepository,recordRepository){this.reports=reportRepository;this.records=recordRepository;}async generate(){const rows=await this.records.summarizeByMonth();for(const r of rows)await this.reports.saveSummary(r.anio,r.mes,r);const h=new Date(),a=h.getFullYear(),m=h.getMonth()+1;await this.reports.removeWithoutRecordsExceptCurrent(a,m);if(!rows.some(r=>Number(r.anio)===a&&Number(r.mes)===m))await this.reports.saveSummary(a,m,{totalRegistros:0,totalGalones:0});}async list(){await this.generate();return this.reports.list();}listGeneral(i,f,b){return this.records.findByDateRange(i,f,b);}}module.exports={ReportService};
