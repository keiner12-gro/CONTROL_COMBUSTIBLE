node_modules/
.env
*.log
.DS_Store
Thumbs.db

# Archivos subidos en local (en produccion se guardan en la base / R2)
uploads/

# Datos locales de prueba (contienen contrasenas en texto plano)
data/usuarios.json
data/registros.json

# Base de datos local embebida (npm run dev:local)
.pglite-data/

# Cualquier .env.* con secretos queda fuera de git (menos el ejemplo)
.env.*
!.env.example
